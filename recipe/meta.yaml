{% set python_min = "3.10.0" %}
{% set name = "revel" %}
{% set version = "0.9.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/revel-{{ version }}.tar.gz
  sha256: fc780dfedb46c4bb614f2ab73380244331584f011c498db362d34bda19a2e45f

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - poetry-core
    - pip
  run:
    - python >={{ python_min }},<4.0.0
    - blessed >=1.19.1,<2.0.0
    - readchar >=4.0.3,<5.0.0
    - colorama >=0.4.6,<0.5.0
    - typing-extensions >=4.10.0,<5.0.0

test:
  imports:
    - revel
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://gitlab.com/mad-moo/revel
  summary: Effortlessly spice up your terminal apps using colors, progressbars & more
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - thewchan
